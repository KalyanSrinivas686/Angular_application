trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

stages:
  - stage: Build
    jobs:
      - job: Build
        steps:
          - task: UseNode@1
            inputs:
              version: '18.x' # Specify the Node.js version
            displayName: 'Install Node.js'

          - script: |
              cd your-subdirectory # Change to your project subdirectory if needed
              npm install -g @angular/cli
              npm install
            displayName: 'Install Angular CLI and Dependencies'

          - script: |
              cd your-subdirectory # Change to your project subdirectory if needed
              ng build --prod
            displayName: 'Build Angular Application'

          - task: PublishBuildArtifacts@1
            inputs:
              PathtoPublish: 'your-subdirectory/dist' # Adjust this path as necessary
              ArtifactName: 'drop'
              publishLocation: 'Container'
            displayName: 'Publish Build Artifacts'
